version: '3.8'
services:
  dev:
    build:
      context: ..
      dockerfile: container/Dockerfile.dev
    image: knowledge-dev:latest
    container_name: knowledge-dev
    volumes:
      - ..:/app:Z
    working_dir: /app
    command: ["zsh"]
    tty: true
    stdin_open: true
